general:
  branches:
    ignore:
      - gh-pages

machine:
  timezone: Asia/Tokyo

dependencies:
  pre:
    - npm i -g gatsby@v0.7.7

test:
  override:
    - echo 'test'

deployment:
  production:
    branch: master
    commands:
      - git config --global user.email "ide.yuta@gmail.com"
      - git config --global user.name "Yuta Ide"
      - npm run build
      - npm run deploy
