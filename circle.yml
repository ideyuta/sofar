machine:
  timezone: Asia/Tokyo

dependencies:
  pre:
    - npm i -g gatsby

test:
  override:
    - echo 'test'

deployment:
  production:
    branch: master
    commands:
      - npm run build
      - npm run deploy
